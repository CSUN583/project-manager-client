import React from 'react';
import {Typography} from "@mui/material";

const TeamProjects = () => {
    return (
        <Typography
            variant='h5'
        >
            Projects
        </Typography>
    );
};

export default TeamProjects;
